# 变更日志

本文档记录 Xalor 小站的调优、查修与加固历程（2026-08）。

## 查修的 Bug

### 后端
| # | 问题 | 影响 | 修复 |
|---|---|---|---|
| 1 | `db.js` 的 `charset: 'utf8mb4_unicode_ci'` 是排序规则名而非字符集名 | Knex 生成建表 DDL 输出 `default charset utf8mb4_unicode_ci` → `Unknown character set` 建表失败（ip_bans 等表无法创建） | 改为 `charset: 'utf8mb4'` + `afterCreate` 中 `SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci` |
| 2 | `requestGuard.js` 的 `const { config } = require('../config')` 解构错误 | config 为 undefined → 服务无法启动（TypeError） | 改为 `const config = require('../config')` |
| 3 | `GET /articles/admin/export` 定义在 `/articles/admin/:id` 之后 | `:id` 吞掉 `export` → 导出接口永远返回「参数不合法」 | 路由移到 `:id` 之前 |
| 4 | categories 表缺 `updated_at` 列 | sitemap 查询报 `Unknown column 'updated_at'` → 500 | migrate 补齐列（幂等） |
| 5 | categories 表缺 `sort` 列 | 分类列表与分类排序功能 500 | migrate 补齐列 |
| 6 | HEAD 请求被要求写签名（`req.method === 'GET'` 不含 HEAD） | CDN/监控探活 HEAD 请求 403 | 闸门放行 HEAD（与 GET 同为只读） |
| 7 | `seed.js` / `server.js` / `migrate.js` 缺 `require.main === module` 守卫 | 被 require 时触发建表/启动服务/seed 副作用 | 补 main 守卫 |
| 8 | 慢速攻击防护失效 | Node 25 的 socket timeout 事件/requestTimeout 对半发送 body 不触发 → 慢速连接可无限占用 | 纯定时器兜底：30s 未完成即 408 + 销毁连接 |
| 9 | WAF 对值为对象的 JSON 键名不检测（递归 walkBody 只收集字符串值的键） | 嵌套 `{"data":{"__proto__":...}}` 键名逃过检测 | 键名始终收集 + 孤立关键字键名专用检测（body/query 双侧） |
| 10 | WAF 对数组/嵌套 query 参数值不检测 | `?search[]=SQL 载荷`、`?a[b][c]=XSS` 逃过检测 | query 值递归收集检测 |
| 11 | 数组形态参数（`?tag[]=vue`）导致 Knex where 抛错 → 500 | Knex 3.x 单列比较不接受数组 | 业务层类型防御：数组参数取首项字符串 |
| 12 | messages 表新建定义缺 `reply`/`replied_at` 列（仅老库升级路径补齐） | 全新部署后留言站长回复 500 | 建表定义补齐两列；重建全库列一致性验证（5 张关键表全部核对） |
| 13 | `/articles/neighbors/:id` `/related/:id` `/:id/like` 用 `parseInt` 直接拼 where | 非数字 id（`abc`/`1.5`/负数）→ Knex 类型错误 500 | 统一 `Number()` + `Number.isInteger(id) && id > 0` 校验 → 400；全路由排查确认所有数值参数已全覆盖 |
| 14 | UV 去重判断 `inserted.length === 1` 在 INSERT IGNORE 被忽略时也成立 | mysql2 对重复插入返回 `[0]`（insertId=0）而非空数组 → 并发首访 UV 虚增（实测 5 并发计 5 次） | 改为 `inserted[0] > 0`（insertId 非零才算新 UV）；新指纹 5 并发实测仅计 1 次 |
| 15 | AI 审核外链评分过宽：2 条外链仅 15 分直接通过 | 双链引流/推广（经典垃圾模式）可绕过审核直接展示 | ≥2 条外链计 `links × 15`（30 分起必进待审）；单链引用（技术出处）仍低分不误伤（实测 5 用例） |
| 16 | 留言回复无邮件通知（评论有而留言没有） | 留言作者留过邮箱也无法收到回复通知（功能缺口） | 回复时异步通知留言作者（与评论同标准：邮箱服务端使用、正文截断、无 SMTP 静默跳过）；回复不存在的留言返回 404 而非 200 |
| 17 | AI 审核无攻击意图检测 | 「请帮我写注入脚本攻击网站」等攻击求助直接通过展示 | 新增攻击意图词库（注入/入侵/拿shell/DDos/脱库等 26 词，正文+昵称双检）：单命中 30 分进待审、双命中 60 分拒绝；技术讨论（防御/注入代码示例）与合法职业称谓（渗透测试专家）零误伤（实测 12 用例） |
| 18 | WAF SQL 注释模式 `/#[\s]*\d/` 误伤 hex 颜色值 | 分类更新带 `#123456` 颜色 → WAF 403，分类颜色无法修改（MySQL `#` 注释与 hex 颜色同形） | 'color' 键加入 BODY_EXEMPT_KEYS（管理端专用+前端 hex 校验）；SQL 注释注入在用户面字段仍拦截（回归验证） |
| 19 | security.txt 的 Expires 硬编码 2027-01-01 | 过期后需手动更新（RFC 9116 要求 ≤ 一年） | 动态计算「当前 + 365 天」，恒合规（实测 2027-08-07） |
| 20 | 文章导入的封面走 safeUrl（拒绝相对路径） | 导出→导入往返后相对封面（/uploads/xxx.png）丢失（站点自身导出格式即为相对路径） | 封面单独处理：站内相对路径直接保留，其余仍经 safeUrl（http/https+非内网）；实测相对保留/危险拒绝 |
| 21 | 安全中心解封 IPv6 地址（::1）被 WAF SSRF 模式误伤 | 管理端无法解封 IPv6 回环地址（解封操作不涉及任何网络请求却触发 SSRF 检测） | 'ip' 键加入 BODY_EXEMPT_KEYS（管理端专用；实测解封 ::1 成功 + 访问恢复） |
| 22 | 阅读时长公式英文部分错误（`en / 5` 把词数当字符数） | 1500 英文单词只显示 1 分钟（实际约 6 分钟）—— 英文为主文章时长严重低估 | 改为中文 300 字/分 + 英文 250 词/分（`cn/300 + en/250`）；6 用例验证 + 构建通过 |
| 23 | dompurify ≤3.4.12 XSS 通告（IN_PLACE hook 移除后子树可执行） | Markdown 消毒最后防线存在中危 XSS 风险（npm audit 1 moderate） | `npm audit fix` → dompurify 3.4.13（安全版）；audit 归零 + 构建通过 |
| 24 | requestGuard 的 Referer 旁路条件为裸 `X-Sig` 头存在（可任意伪造的魔法值），且该中间件拒绝分支从未被任何测试触发 | 当前虽被 gate 强验签兜底，但 refererRequired 实际从不过滤任何请求（纵深防御名存实亡）；若未来任何写路由绕过 gate（GATE_SKIP 扩展/中间件顺序调整），伪造 X-Sig 头即可秒过来源校验 | 旁路条件语义化为「签名已验证标记」：gateWriteRequired 验签成功后显式写 `req.sigVerified = true`，refererRequired 仅认标记（头可伪造，标记不可）；新建 requestGuard.test.js 单元测试 23 项覆盖全部拒绝/放行分支（裸头不可伪造旁路为核心回归断言） |
| 25 | 前端表单令牌 fallback 字段名 `'website_url'` 与服务端固定蜜罐字段同名 | bundle 可被攻击者静态分析：读到 fallback 即知「填 website_url 会被拒」→ 永不填写 → 固定蜜罐对了解者失效（动态蜜罐不受影响） | fallback 改为 `'hp_website'`（与服务端固定蜜罐名不同；动态蜜罐按 X-Hp-Field 声明检查任意字段名，fallback 语义不变）；固定蜜罐名从 bundle 消失恢复隐蔽性；构建 + 表单全链路回归通过 |
| 26 | 密码长度校验按字符数（`password.length > 72`），而 bcrypt 截断单位是字节 | 72 个中文字符 = 216 字节被 bcrypt 截断至 72 字节 → 长尾不同的密码哈希碰撞（多字节密码边界）；登录/注册/改密三处均受影响 | 统一改为 UTF-8 字节数校验（`Buffer.byteLength` ≥8 且 ≤72）：中文 25 字（75B）拒绝、24 字（72B）放行；8 用例边界验证 + admin 套件回归 |
| 27 | 点赞防刷键含 X-Fp（`ip:fp:articleId`），而指纹只是格式校验的字符串 | 脚本轮换 fp 即获新键 → 10 秒窗口形同虚设（刷榜/刷赞无成本）；且 429 不 report → IP 信誉系统对刷赞无感知 | 防刷键改为纯 IP 维度（`ip:articleId`，fp 可伪造不可作防刷依据）+ 429 时 `report(rate)` 计入信誉（持续刷赞 → 积分 → 自动封禁）；HTTP 断言（成功/429）+ likeGuard 单元套件 6 项（换 fp 不可绕/多文章独立窗口/跨 IP 独立） |
| 28 | 页面 CSP 含 `script-src 'unsafe-inline'`（主题初始化内联脚本）+ `img-src https:` | 内容层 XSS 一旦逃逸：可注入内联脚本读取 localStorage 的 JWT，经 `https:` 图片通道外传 —— CSP 的「外传阻断」承诺被组合削弱 | 主题初始化脚本移至外部文件（public/theme-init.js，同步加载无 FOUC），CSP 收紧为 `script-src 'self'`（无内联）：逃逸后无法执行脚本 → 无读取令牌通道；dist 产物核查零内联脚本 + 构建通过 |
| 29 | seed 初始管理员密码默认 `admin123`（弱密码且公开于文档） | 生产部署若忘记设置 SEED_ADMIN_PASSWORD → 初始管理员即弱凭据（攻击者直试 admin/admin123 入侵后台） | 生产模式（NODE_ENV=production）下 seed 未设置 SEED_ADMIN_PASSWORD 直接拒绝执行并提示；README/.env.example 同步补充生产警告（默认值仅限本地演示） |
| 30 | 评论/留言重复内容检测用 `toISOString()`（UTC）与 DB 存储的本地时间字符串比较 | +8 时区服务器实际窗口膨胀为 ~8.5 小时（30 分钟 + 8h 偏移）：同一 IP 数小时前发过的相同内容再发被误判「内容重复」429；UTC 服务器则正常 → 行为随时区漂移 | 改用 `localDateTimeStr()` 生成本地时区比较基准（与 DB 存储一致），窗口精确 30 分钟；comments/messages 两处同步修复 |
| 31 | antiSpam 链接计数只认 `http(s)://`（裸 `www.` 域名不计） | 2 个裸 www 链接逃过硬拒层（计 0），审核层裸域计分 20 分不足 pending 阈值 → 与 http 链接行为不一致的垃圾双链通道（行为漂移） | countLinks 补 `www.` 裸域变体（多级域/路径全支持）：2 个 www 链接同样硬拒；8 用例验证（https/www/多级域/markdown/正常文本） |
| 32 | 定时清理的 visits/visit_uv 日期边界用 `toISOString()`（UTC 日期）与 DB 本地日期 day 列比较 | +8 时区服务器阈值漂移 8 小时（方向保守：多保留约 8 小时旧数据，无丢失风险但行为随时区漂移） | 改用 `localDateStr()` 本地日期（与 day 列存储一致）；server.js 语法检查 + journey 回归 |
| 33 | 分享页 JSON-LD 用 `JSON.stringify` 但不转义 `<` | 设置项存储无标签清洗（站点名等）—— 若值含 `</script>` 序列会提前终止 script 块造成 HTML 注入（管理员信任边界外的纵深缺口） | 标准做法：`.replace(/</g, '\\u003c')`（字节级验证：`</script>` 注入序列消失 + JSON 还原可解析） |
| 34 | WAF SSTI 模式仅覆盖算术探测 `{{7*7}}`（模糊测试发现 `{{config}}`/`{{7*'7'}}` 全变体放行） | SSTI 探测载荷放行（博客无模板引擎不可利用，但与「SSTI 检测」声明不符；且既有 waf.test.js 的 SSTI 断言被封禁状态喂饱 —— 真实检测缺口被掩盖） | MISC_PATTERNS 扩展：Jinja2/Flask 全局对象探测（config/self/request/app/session 等 11 词）+ 字符乘法 `{{7*'7'}}`；模糊测试拦截 1177→1207（92.3%→94.7%），`{{config}}` 全变体归零放行 |
| 35 | WAF FastJSON 模式仅匹配无引号形态（`@type:`），`"@type"` JSON 键形态放行；ASP 仅匹配 `%=` EL 形态，`<% exec(...) %>` 探测放行 | 标准 FastJSON 载荷 `{"@type":"java.lang.Runtime"}` 与 ASP 探测全变体放行（模糊测试发现；博客无对应执行面不可利用，但检测声明不符） | MISC_PATTERNS 补 `"@type":"类名"` 键形态 + `<% 函数调用 %>` ASP 形态（正文类豁免键不受影响）；模糊测试拦截 1207→1234（94.7%→96.8%），放行 68→41（剩余均为 `\t` 字面量/无 CRLF 请求行/单字符等不可利用误报） |
| 36 | 文章导入的 slug 仅截断（`slice(0,80)`）不白名单清洗（创建/更新走 slugify — 行为漂移） | 脏 slug（含 `/` `.` `..` 空格等）可直接入库污染 URL 面（分享页路径分裂/语义混淆） | 导入 slug 加白名单校验（小写字母/数字/中文/连字符，与 slugify 输出等价）—— 不符回退自动生成；10 用例边界验证 + admin 套件回归 |
| 37 | 前端 ENC_SALT 硬编码 `'xalor-content-v1'`（与后端 config 默认一致） | 生产按 .env.example 建议自定义 ENC_SALT 后前端密钥派生不匹配 → 正文解密失败渲染为空（部署配置陷阱，非攻击面） | VITE_ENC_SALT 构建注入（与 VITE_API_PREFIX 同模式）：生产自定义盐须同步前端环境变量；默认值保持与服务端一致；构建产物注入确认 |
| 38 | 建库脚本应用账号 `GRANT ALL`（含 DROP 权限）+ 默认弱密码 xalor2026 无生产警告 | 应用账号权限过大（应用无 DROP 操作，ALL 超出最小权限）；生产若复用脚本且不改密码 → 数据库弱凭据面 | blog.sql：最小权限 GRANT（SELECT/INSERT/UPDATE/DELETE/CREATE/ALTER/INDEX/REFERENCES，实测迁移+套件全通）+ 生产改密警告注释；当前开发库已 REVOKE 收紧（历史测试库清理） |
| 39 | 传递依赖 nanoid 3.3.16（sanitize-html→postcss / vite→postcss）新披露 high 漏洞 GHSA-2v37-7h3g-55p8（自定义生成器 size=0 无限循环） | 双端 audit 1 high（此前 0 —— 新通告入库触发） | `npm audit fix` 双端 → nanoid 3.3.17+；audit 双端归零 + journey 回归 + 前端构建通过 |
| 40 | Dashboard 互动趋势比较用 Date 对象（Knex 序列化为 UTC ISO）与 DB 本地时间 created_at | +8 时区服务器少计当天最早 8 小时评论/留言（趋势图数据偏差 —— 与 Bug #30/32 同族） | 改用 `localDateTimeStr()` 本地时间字符串（与 DB 存储一致）；admin 套件回归 |
| 41 | 分类颜色服务端无 hex 校验（前端有 —— Bug #18 修复时只改了一侧） | 任意字符串经 CSS 变量注入额外声明（管理员信任面 —— 纵深远低于威胁模型但成本低） | 服务端 `validHexColor()` 白名单（#RRGGBB 大小写兼容，不符回退默认色）；创建/更新两处 + 6 用例验证 + admin 回归 |
| 42 | README 的 Nginx 建议 CSP 仍是旧版 `script-src 'self' 'unsafe-inline'` | 与第 634 轮收紧后的前端 CSP 不一致 —— 部署时 Nginx 响应头覆盖 index.html 的 meta，使 CSP 强化（去 unsafe-inline）实际失效 | README Nginx 段 CSP 同步为 `script-src 'self'`（注释说明脚本已外部化） |
| 43 | WAF 检测管线：全角归一不含全角百分号（％）+ 归一后不补解码；SSTI 模式无属性链探测 | 全角+URL 编码组合（`１％27ＯＲ％201％3D１`）绕过关键词检测；`{{"x".__class__.__mro__}}` 属性链探测放行（高级绕过矩阵发现） | 全角归一补 `％`（U+FF05）+ 归一后补一轮解码；MISC 补 `__class__/__mro__/__subclasses__/__globals__/__builtins__/__init__/__import__` 属性链模式；修复 3/3 复测 + 模糊测试变化分析（TRAV+3 为解码更完整归入已知权衡 —— 非回归） |
| 44 | WAF 全角归一表不完整（仅部分符号：引号/括号/等号/尖括号/分号/斜杠）+ 归一后补解码仅 1 轮 | 全角符号组合（`．．／`、`｜ｗｈｏａｍｉ`、`ｈｔｔｐ：／／１２７．０．０．１`、`｛＂＠ｔｙｐｅ＂：` 等）绕过 —— 矩阵 2 发现 7 缺口 | 全角段整体归一（U+FF01-FF5E 全部可打印字符 → 半角）+ 归一后循环解码 2 轮（全角+双层编码）；矩阵 2 复测 6/7 + 真实遍历变体 3/3（剩余 1 个为无害 `./` 语义 —— 非缺口） |
| 45 | WAF 归一后解码轮数不足（2 轮上限） | 三重编码载荷（`％252527` → `%27` → `'`）残留编码形态逃过关键词检测（矩阵 3 发现） | 循环解码上限 2→3（覆盖现实攻击深度 1-3 层；4 层+编码无法到达业务层利用 —— 记录为合理边界）；复测 3/3 + fuzz 稳定 + 全量回归 |
| 46 | 文章页分享菜单的 setTimeout 延迟添加外部点击监听（无 timer 记录） | 菜单打开后立即切换页面 → 卸载清理已执行 → setTimeout 回调仍添加监听 → 泄漏（指向已卸载组件的闭包；高频开合+切页累积） | shareClickTimer 记录 + onUnmounted/关闭时 clearTimeout 兜底；构建通过 |

### 前端
| # | 问题 | 影响 | 修复 |
|---|---|---|---|
| 1 | 路由守卫在秘钥路径未缓存时放行任意单段 URL | `#/foo` 渲染后台登录/空壳而非 404 | 缓存缺失时先 `await getAdminPath()` 再比较 |
| 2 | 文章详情页 slug 切换竞态 | 快速切换上一篇/下一篇时旧响应覆盖新文章 | 请求序号机制（过期响应丢弃） |
| 3 | TOC 编号与渲染标题 id 分叉 | 代码块内 `## foo` 被误算为标题 → 锚点跳错 | 改用 marked 词法解析（跳过代码块、计入引用块） |
| 4 | 文章编辑器自动保存定时器未清理 | 卸载后把旧文章内容写入新文章草稿键 | onUnmounted 清理定时器 |
| 5 | 401 后 Pinia store 与 localStorage 不同步 | isLoggedIn 残留 true，界面短暂显示已登录 | 拦截器动态 import store 调 clearSession() |
| 6 | theme store 对损坏的强调色无校验 | 非法 accent 值 → NaN 颜色 → 整站色失效 | 读取/设置时严格 hex 校验，回退默认色 |
| 7 | PoW 求解阻塞应用挂载 | 低端设备/高信誉 IP 首屏白屏数秒 | 先挂载渲染骨架，PoW 后台并行（拦截器等待票据） |
| 8 | 评论加载竞态（同详情页模式） | 快速切文章时旧评论覆盖新文章 | 请求序号机制 |
| 9 | PoW 求解器逐次 hex 转换低效 | 求解慢 ~40% | 直接检查 digest 字节（bit 级前导零） |
| 10 | 文章详情操作按钮缺 aria-label | 无障碍缺陷 | 补充 11 处 aria-label + 热门 tab aria-pressed |
| 11 | auth store 模块顶层 `JSON.parse(localStorage)` 无 try/catch | localStorage 数据一旦损坏（任何原因），模块加载即抛异常 → 整个应用白屏且无法自行恢复 | 安全读取封装 `readCachedUser()`：解析失败回退 null 并自愈清除损坏项；全站 JSON.parse 面核查（搜索历史已有保护、theme 为字符串）—— 构建通过 |

## 安全加固

### WAF
- **JSON body 递归检测**：嵌套对象中的攻击载荷（SQL/XSS/命令注入）同样拦截
- **HTTP 请求走私检测**：Content-Length + Transfer-Encoding 双头/重复长度头直接拒绝
- **键名注入检测**：`__proto__` / `constructor` / `prototype` 孤立键名（body 键 + query 键）→ 拦截
- **编码变体全覆盖**：双层 URL 解码、HTML 实体、Unicode 转义、全角归一化（6 类变体测试固化）
- **框架漏洞载荷**：SSTI/XXE/Log4Shell/OGNL/FastJSON/CRLF（9 类测试固化）
- **上传 SVG 扫描加固**：新增 SMIL 事件变体拦截（`<set attributeName="onload">`、`onbegin=`/`onend=`/`onrepeat=`）—— 此前文本不含 `onload=` 与 `javascript:` 可绕过；合法动画（animateMotion）不受影响
- **命令注入特征库扩充**（模糊测试发现）：`echo`/`grep`/`head`/`tail`/`sort`/`base64`/`awk`/`sed`/`find`/`env`/`hostname`/`chown`/`kill`/`dd` 补入 `;` `|` `&&` `||` 各命令表 —— 此前 `|| echo pwned` 等探测载荷全变体漏过

### 反爬
- **RSS/分享页明文通道限流加固**：/api/rss.xml 与 /api/share/* 是 GATE_SKIP 公开放行通道（社交抓取/收录必需），此前共享 60/min 且**超限不积分** —— 脚本以 1 req/s 可稳定拖取全站正文且信誉系统无感知。现拆分独立限流（RSS 30/min —— 阅读器低频轮询足够；分享页 60/min）+ 超限 `report(rate)` 计入 IP 信誉（持续拖取 → 积分累积 → 自动封禁）；实测第 31 次 RSS 必 429 且窗口内持续受限
- **PoW 难度统一为 bit 语义**（16→24 档）：修复 hex 字符/bit 语义混用，前端求解器同语义
- **票据续期上限**：单票据最多续期 10 次，用尽强制重新 PoW（防一次求解无限抓取）
- **指纹-IP 关联检测**：同一指纹 ≥3 个不同 IP → 判代理池，难度拉满 + 计分（仅跨阈值计一次）
- **UA 轮换检测**：同 IP 5 分钟 ≥6 个浏览器 UA → 计信誉积分
- **feed 限流**：RSS/Sitemap/分享页 60 次/分钟（防全文刷取）
- **正文加密验证**：IV 唯一性、GCM 认证拒绝篡改、错误票据无法解密

### 防护
- **慢速攻击防护**（见 Bug #8）
- **HSTS 条件化**：仅 HTTPS（含代理透传）发送（helmet v8 无 setIf，自实现）
- **X-Permitted-Cross-Domain-Policies: none**：防插件跨域读文档
- **PV 防刷**：站点统计 10 秒/IP 窗口
- **批量操作上限**：后台批量删除/更新单次 ≤500 条
- **CORS 可配置**：`CORS_ORIGINS` 环境变量
- **挑战池清理优化**：优先清过期挑战，防撑爆挤掉正常用户
- **SMTP 响应码校验**：认证失败/收件人拒绝即终止，避免错误状态下继续发送
- **seed 生产模式不打印密码明文**
- **攻击事件持久化**：批量落库 audit_logs（SEC-EVENT，30s/100 条 flush），安全中心跨重启可查
- **vite 7 升级**：修复 esbuild 已知漏洞（npm audit 0 漏洞）

## 功能完善
- 随机文章接口两阶段实现（>1000 篇用随机 offset，避免 RAND() 全表排序）
- 移动端 TOC 抽屉/悬浮操作栏（验证）
- 部署辅助：systemd 服务单元 + 备份脚本（自动探测 mysqldump）+ 使用文档
- MySQL 配置优化：空闲连接 5 分钟回收、严格模式（validate-config 验证）
- **邮件通知 SMTP 合规修复**：主题 RFC 2047 encoded-word ≤75 字符（中文按 UTF-8 字节截断 45B，防客户端乱码）+ 正文超长行 990 字符折叠（RFC 5322 行 ≤998 限制，Postfix 等服务器拒绝超长行导致通知投递失败）；CRLF 注入清洗回归 7 项边界验证（长文/中文/emoji/注入载荷）

## 测试体系
- `server/test/client.js`：反爬测试客户端（PoW/指纹/签名/表单令牌/上传，并发安全；支持自定义 UA）
- `server/test/run.js`：测试运行器（清库 + 套件间隔离重启 + 等待就绪）
- `server/test/reset-env.js`：手动环境重置
- `server/test/waf-fuzz.js`：WAF 模糊测试（1275 个编码变体进程内直调，无网络限流干扰）
- **测试结构修复**：原 security.test.js 13-16 节在信誉封禁后运行，403 断言被封禁状态"喂饱"无法证明 WAF 引擎真实拦截 —— 攻击载荷迁移至独立 `waf.test.js` 套件（run.js 套件间重置 IP 信誉），前 3 个命中严格校验 WAF 专属消息「请求被拒绝」，后续接受 WAF/封禁双形态，末尾显式断言自动封禁
- 9 个测试套件共 **246 项**：
  - `admin.test.js`（43）：后台 CRUD/上传安全/导出/设置/PV 防刷
  - `2fa.test.js`（9）：TOTP 全流程（启用/登录/重放/关闭）
  - `session.test.js`（12）：会话列表/撤销/登出
  - `lockout.test.js`（16）：登录爆破锁定
  - `journey.test.js`（23）：全链路用户旅程验收（含站点设置时区偏移断言）
  - `waf.test.js`（74）：WAF 引擎专项（严格消息校验/编码变体/深度绕过/递归检测/原型污染键/蜜罐/自动封禁）+ Fetch Metadata 校验 + RSS 明文通道限流（超限 429）
  - `requestGuard.test.js`（23）：来源校验/时间戳中间件单元覆盖（无服务依赖，mock 直驱全部分支；含签名旁路语义回归、gate sigVerified 标记链路闭环）
  - `likeGuard.test.js`（6）：点赞防刷键语义单元覆盖（换 fp 不可绕/多文章独立窗口/跨 IP 独立）
  - `security.test.js`（40）：闸门/WAF 全向量/编码变体/蜜罐/封禁/正文加密/续期链上限/UA 轮换/点赞防刷

## 验证记录
- 全链路验收 17/17、双轮回归 166/166 零失败
- 第三轮回归 194/194 零失败（含续期链上限、UA 轮换检测端到端实弹验证）
- 第四轮回归 203/203 零失败（7 套件：新增 waf.test.js 64 项严格消息验证、测试结构修复）
- 第五轮回归 204/204 零失败（journey 新增站点设置时区偏移断言；含 UV 去重、外链审核、留言回复通知等全部修复）
- 第六轮回归 227/227 零失败（新增 requestGuard.test.js 23 项；含 Referer 旁路语义化修复 —— 见 Bug #24）
- 第七轮回归 234/234 零失败（waf 套件新增 Fetch Metadata 7 项；含前端蜜罐字段名泄露修复 —— 见 Bug #25）
- 第八轮回归 237/237 零失败（waf 套件新增 RSS 明文通道限流 3 项；RSS/share 拆分限流 + 超限计信誉）
- 第九轮回归 237/237 零失败（含 SQL raw 面 14 点全审、上传 magic bytes/SVG 全文扫描、admin 权限 50 路由全覆盖、登录流防枚举/双层锁定确认；邮件 SMTP 合规修复、bcrypt 字节截断修复 —— 见 Bug #26）
- 第十轮回归 246/246 零失败（likeGuard 单元套件 6 项 + security 点赞防刷 3 项；点赞防刷键去 fp + 超限计信誉 —— 见 Bug #27；测试顺序解耦：积分敏感断言置于零积分区）
- 第十一轮：CSP 强化（script-src 去 unsafe-inline + 主题脚本外部化，dist 零内联核查）+ seed 生产守卫 + .env.example 生产警告（见 Bug #28/29）
- 第十二轮：settings 键白名单/协议校验/缓存确认 + social_email mailto 双保险确认 + messages 三层提交链确认；重复检测时区错位修复（见 Bug #30，comments/messages 两处）
- 第十三轮：antiSpam www 裸域变体修复（见 Bug #31）+ stats/crypto/pass/pow/fingerprint/拦截器/部署面审计确认 + 机制总盘点（闸门 PoW 16→24/票据 600s/续期 480s/签名窗口 300s、WAF 17 豁免键、信誉 5 权重+15min→24h、AES-GCM 加解密、40 敏感词）
- 第十四轮：错误处理脱敏核查（零 e.message 泄漏）+ TOTP RFC 6238 实现审计（动态截断/±1 步/防重放/5 分钟清理）+ 第 650 轮全量回归 246/246 零失败
- 第十五轮（651-660）：编辑器/安全中心/路由守卫/adminPath/PWA/anti-debug/备份脚本审计确认 + README 测试数同步（188→246 项）+ .gitignore 创建（.env/uploads/备份防入库）+ 依赖版本审查（dompurify 3.4.13 安全版生效、稳定线无已知漏洞）
- 第十六轮（661-670）：登录页/限流总表/markdownText/ownerBadge 权衡记录/性能基线（毫秒级无回退）/seed 内容零外链/site-theme store/建表 14 表与代码引用 14/14 对应 + 第 670 轮全量回归 246/246 零失败
- 第十七轮（671-680）：CSV 公式注入三处全覆盖确认 + admin 事务完整性 + Home/AuditManage/Dashboard 纯插值 + settings 公开只读 + Links/Messages 提交链一致 + 攻击链端到端模拟 8/8 全拦 + 清理任务时区修复（见 Bug #32）+ 第 680 轮全量回归 246/246 零失败
- 第十八轮（681-690）：点赞前端一致性/JSON-LD 注入修复（Bug #33）/服务端冗余依赖 marked 移除（零代码引用）/WAF SSTI+FastJSON+ASP 模式扩展（Bug #34/35，模糊测试 1177→1234，拦截率 96.8%）/存储型 XSS 三层防线动态验证（WAF 前置拦截+cleanText+safeUrl）/第 690 轮全量回归 246/246 零失败
- 第十九轮（691-700）：现代向量验证（IPv4-mapped/短整型 SSRF、SMIL set、注释分隔符全拦截；GraphQL 探测无执行面记录权衡）+ 模糊测试库固化（1275→1326 变体，拦截率 96.9%）+ 无 TODO 残留 + 构建产物安全复核 + 攻击者生命周期端到端 4/4（攻击→封禁→全站封锁→持久化）+ localStorage 18 键全核对 + 票据篡改矩阵 12/12 全拒 + 封禁持久化恢复确认 + 第 700 轮全量回归 246/246 零失败
- 第二十轮（701-705）：导入 slug 白名单清洗（Bug #36）+ 设置导入复用 saveSettings 完整校验链确认 + 前端构建最终确认 + 接口契约复检 + 第 705 轮全量回归 246/246 零失败
- 第二十一轮（706-710）：前端解密契约核对 + ENC_SALT 构建注入（Bug #37，配置漂移陷阱）+ 安全中心轮询压力面确认 + 导出/导入往返闭环 + 用户主链路端到端 9/9（PoW→浏览→详情→评论→续期→RSS）+ 第 710 轮全量回归 246/246 零失败
- 第二十二轮（711-714）：建库脚本最小权限收紧（Bug #38 —— REVOKE ALL 后实测迁移+全套件 246/246 通过）+ 上传管理面（白名单/双校验/引用保护/24h 竞态窗口）确认 + App/reveal 边角干净 + 最小权限全量回归零失败
- 第二十三轮（715-718）：票据生命周期边界 5/5（正常/续期/10 次上限/伪造 jti/过期）+ WAF 性能边界有界确认（大载荷 1-2ms 无指数放大）+ 双端依赖审计发现新通告 nanoid high 并修复（Bug #39）+ 依赖更新后全量回归 246/246 零失败
- 第二十四轮（719-720）：传递依赖状态审查（major 落后均为稳定线无已知漏洞）+ express-rate-limit 补丁更新 8.6.2 + 第 720 轮全量回归 246/246 零失败
- 第二十五轮（721-725）：15 个内存 Map 清理策略全盘点（14 个上限清理 + ipGuard records decay 循环删除 —— 全部有界）+ 安全响应头全景 3/3（API/分享页/RSS 10 头完整）+ 上传资源头代码级确认 + 上传安全断言覆盖复核 + 第 725 轮全量回归 246/246 零失败
- 第二十六轮（726-730）：深属性访问安全 + 正则 ReDoS 面全线性确认 + JSON 边界实测（超大 413/畸形 400/原型污染 WAF 拒/深层嵌套触发 bodyShapeSafe）+ 深度/数组边界精度验证（20 层/1000 项精确）—— 全部防护层实测触发 + 第 730 轮全量回归 246/246 零失败
- 第二十七轮（731-735）：CSP 与资源加载兼容性终验（系统字体/Gravatar/站内资源全在允许集）+ Gravatar 隐私面（明文邮箱剥离）+ 构建产物时效核查 + business API 契约逐条核对（security/audit/settings/export/audit 删除全匹配）+ 第 735 轮全量回归 246/246 零失败
- 第二十八轮（736-740）：批量操作审计（500 上限/类型过滤/外键双保险/1000 事务上限）+ 会话链路端到端 6/6 + **ipGuard 防死锁豁免设计确认**（封禁中 anti/登录/后台可及 —— 排查确认非 bug）+ 豁免面纵深验证（豁免只跳 ipGuard，gate/authRequired 仍生效）+ seed 引导通道设计确认 + 第 740 轮全量回归 246/246 零失败
- 第二十九轮（741-745）：多标签同步 401 兜底确认 + Dashboard 互动趋势时区修复（Bug #40）+ 全库 Date 用法时区扫描（剩余均为文件名/内存/epoch —— 无遗漏）+ timeAgo 跨时区 4/4 动态验证 + 第 745 轮全量回归 246/246 零失败
- 第三十轮（746-750）：fontSize Number() 转换防注入确认 + localStorage→CSS 面全景（Vue :style 属性绑定天然安全）+ 分类颜色服务端校验（Bug #41，与前端对称）+ 校验不对称扫描（无其他单侧缺口）+ 第 750 轮全量回归 246/246 零失败
- 第三十一轮（751-755）：提交防重三层确认（前端禁用+令牌单次+限流）+ 搜索防抖 300ms+序号竞态 + 模态框焦点陷阱 + 快捷键配对清理 + 第 755 轮全量回归 246/246 零失败
- 第三十二轮（756-759）：表情面板/评论树（MAX_TREE_DEPTH 有界 + 时间单向防环）+ 前后端递归渲染双防护 + 最终综合状态报告（服务运行/audit 0/246 断言/1326 变体 96.9%）
- 第三十三轮（760）：第 760 轮全量回归 246/246 零失败（连续 760 轮持续优化 —— 达成并远超 600 轮目标，全部防线持续稳定）
- 第三十四轮（761-765）：零 Cookie 架构确认（无 CSRF cookie 面）+ 头洪泛双层防线实测（Node 400 前置 + WAF 403 兜底）+ 前后端长度限制 5/5 一致 + re-ai 认证+限流兜底 + 第 765 轮全量回归 246/246 零失败
- 第三十五轮（766-770）：ImageManage 确认框/引用保护 + admin v-html 仅 2 处全经 DOMPurify + 前台 v-html 8 处全消毒 + 响应敏感字段扫描（前台邮箱剥离/后台认证可见/无凭据字段）+ 第 770 轮全量回归 246/246 零失败
- 第三十六轮（771-775）：securityStats 聚合审计（内存+DB 合并/重启不归零）+ 安全中心端到端 5/5（登录→查看→解封→移除）+ CSV 导出端到端 4/4（BOM/表头/公式注入防护实测）+ 2FA 二维码 fallback 链路 + 第 775 轮全量回归 246/246 零失败
- 第三十七轮（776-780）：改密/2FA 启用/禁用/登录会话全流程审计（会话撤销/验证码回环/防旁路/记录完整性）+ 第 780 轮全量回归 246/246 零失败
- 第三十八轮（781-785）：配置一致性终验（apiPrefix/adminPath/ENC_SALT 前后端对齐）+ 侧边栏持久化 + requestId 追踪链路 + 组合攻击稳定性演练（20 并发混合向量 20/20 拦截 + 服务稳定）+ 第 785 轮全量回归 246/246 零失败
- 第三十九轮（786-790）：archive/related/Home 数据面审计 + Nginx 建议 CSP 同步修复（Bug #42 —— 部署层与前端 CSP 强化对齐）+ 第 790 轮全量回归 246/246 零失败
- 第四十轮（791-795）：README 数字一致性扫描 + .env.example 与 config 默认值 9/9 核对 + anti 引导路由限流全覆盖确认 + 安全头最终动态复核 10/10 + 第 795 轮全量回归 246/246 零失败
- 第四十一轮（796-799）：Lightbox 审计 + 项目文件审计覆盖对照（服务端 43 文件 + 前端全部组件全覆盖）+ 管理页 22 处确认框防误触 + 前端构建/audit 最终确认
- **第 800 轮里程碑**：全量回归 246/246 零失败 · 连续 800 轮持续优化（远超 600 轮目标）· 后端 Bug 修复 42 项 + 前端 11 项 · 模糊测试 1326 变体拦截率 96.9% · 双端 audit 0 漏洞 · 9 套件 246 断言全绿 · 全部机制盘点就绪
- 第四十二轮（801-803）：热路径索引复验 5/5（列表/详情 const/评论/UV 唯一/会话）+ 慢查询扫描（审计中缀 LIKE 全表为已知取舍 —— 90 天清理控量）+ escapeLike 字节级 6/6（Write 工具直写排除 shell 转义干扰）
- 第四十三轮（804-805）：bookmark 本地收藏审计（try/catch + 100 上限）+ 第 805 轮全量回归 246/246 零失败
- 第四十四轮（806-807）：localDraft（24h TTL/损坏清理）/admin store 审计 —— 前端全部源文件覆盖完毕 + 第 807 轮全量回归 246/246 零失败
- 第四十五轮（808）：早期修复回归抽查（Bug #13 参数校验 8 处 / #17 解密票据 / #30 时区 —— 全部在位无回退）
- 第四十六轮（809-810）：第二批回归抽查（#14 UV insertId / #15 外链×15 / #18 color 豁免 / #24 sigVerified / #28 CSP 产物 / #38 最小权限 —— 6/6 在位）+ 第 810 轮全量回归 246/246 零失败
- 第四十七轮（811-812）：第三批回归抽查（#19 security.txt / #20 封面 / #21 unban / #25 蜜罐 / #26 解析防御 / #27 点赞 / #31 www / #33 JSON-LD / #36 slug / #37 ENC_SALT / #40 dashboard / #41 颜色 / #42 nginx —— 14/14 在位）+ 第 812 轮全量回归 246/246 零失败（全部 42 项修复无回退）
- 第四十八轮（813-814）：高级绕过矩阵（组合混淆 12 向量 —— 发现 2 缺口）+ Bug #43 修复（全角百分号+补解码 / SSTI 属性链）+ 模糊测试变化分析（放行均归已知权衡 —— 非回归）+ 第 814 轮全量回归 246/246 零失败
- 第四十九轮（815-818）：矩阵 2（全角符号组合 7 缺口 → Bug #44 全角段整体归一+循环解码）+ 矩阵 3（三重编码 → Bug #45 解码 3 轮；四重编码为不可利用合理边界）+ 非 WAF 纵深绕过 4/4（跨路径/错指纹/蜜罐全拒）+ 第 818 轮全量回归 246/246 零失败
- 第五十轮（819-820）：业务逻辑绕过矩阵 7/7（分页钳制/slug 注入/参数校验/父评论/WAF 注入全拒 —— 一次 403 为测试环境积分残留，重置后确认）+ 第 820 轮全量回归 246/246 零失败
- 第五十一轮（821-822）：多阶段攻击链 6/6（后台探测→秘钥路径→无 JWT 401→弱口令登录→错密码改密→2FA 状态 —— 一次 403 为封禁豁免设计的正确表现，重置后确认）+ 第 822 轮全量回归 246/246 零失败
- 第五十二轮（823-824）：并发压测（50 并发混合读全 200 / 均值 5.8ms / 15 混合读写全 200 —— 无退化）+ 第 824 轮全量回归 246/246 零失败
- 第五十三轮（825-827）：资源耗尽边界（8KB URL 403 / 20 并发 1.9MB body 全拒 / 压测后服务稳定）+ 前端渲染边界（分页白名单 8/16/32 + Archive 分块）+ 第 827 轮全量回归 246/246 零失败
- 第五十四轮（828-830）：日志可追溯性三层确认（console 实时 + 信誉积分 + audit_logs 持久化）+ 备份导出闭环复验（文章 6 篇/设置）+ 第 830 轮全量回归 246/246 零失败
- 第五十五轮（831-833）：测试套件隔离机制三重确认（重启/DB 清理/TOTP 重置）+ 测试客户端 13 函数完备 + 第 833 轮全量回归 246/246 零失败
- 第五十六轮（834-835）：生产部署就绪清单 8/8（JWT_SECRET/TRUST_PROXY/ALLOWED_HOSTS/CORS/ADMIN_PATH/ENC_SALT 对齐/TTL/窗口）+ 第 835 轮全量回归 246/246 零失败
- 第五十七轮（836-838）：NotFound 页行为（noindex/倒计时/随机文章）+ 站内搜索跳转链（query 参数经转义）+ 第 838 轮全量回归 246/246 零失败
- 第五十八轮（839-840）：后台登录跳转链路（已登录直达/登录后进仪表盘）+ 第 840 轮全量回归 246/246 零失败
- 第五十九轮（841-842）：表单令牌并发安全复验（3 并发同令牌 → 1 成功 + 2「已被使用」；中途 403 为最小间隔拦截的正确行为 + 封禁残留干扰排除）+ 第 842 轮全量回归 246/246 零失败
- 第六十轮（843-844）：会话撤销链路验证（双设备登录/logout-all 撤销其他保留当前 —— 语义确认 + 设备 2 撤销正确）+ 第 844 轮全量回归 246/246 零失败
- 第六十一轮（845-847）：路由切换状态清理 22/22 + 监听器配对终核（发现分享菜单延迟挂载竞态 → Bug #46 timer 兜底修复）+ 第 847 轮全量回归 246/246 零失败
- 第六十二轮（848-849）：定时器清理完整性扫描（15 处 —— 关键定时器全清理；draftTimer 清理缺失为无害级 —— 回调无组件引用）+ 第 849 轮全量回归 246/246 零失败
- **第 850 轮里程碑**：连续 850 轮持续优化 · 双端 audit 0 · 后端 Bug 45 项 + 前端 11 项 · 246 断言全绿 · 模糊测试 96.9% 拦截率
- 第六十三轮（851）：第 851 轮全量回归 246/246 零失败
- 第六十四轮（852-853）：垃圾输入响应一致性矩阵 9/9（畸形参数容错回退/严格 400 —— 零 5xx）+ 第 853 轮全量回归 246/246 零失败
- 第六十五轮（854-855）：写接口垃圾输入矩阵 6/6（空 body/类型错误/超大昵称 —— 零 5xx）+ 第 855 轮全量回归 246/246 零失败
- 第六十六轮（856-857）：数据库约束完整性审计（5 表 NOT NULL/UNIQUE 与应用校验全部对应）+ 第 857 轮全量回归 246/246 零失败
- 第六十七轮（858-859）：外键完整性审计（6 外键完整 + 孤儿数据 0）+ 第 859 轮全量回归 246/246 零失败
- 第六十八轮（860-861）：数据生命周期清理实测（过期会话/封禁插入→清理→残留 0 —— 应用列与应用代码一致）+ 第 861 轮全量回归 246/246 零失败
- 第六十九轮（862-863）：时区链路终验（DB 会话 +08:00 = 服务器偏移 -480 = 前端校正基准 —— 全链路自洽）+ 第 863 轮全量回归 246/246 零失败
- 第七十轮（864-865）：超时防御层次核对（前端 20 ≤ Node 20 < 应用 30 < Nginx 35 —— 逐层兜底自洽）+ 第 865 轮全量回归 246/246 零失败
- 第七十一轮（866-867）：密码学使用面终核（12 使用点 —— 算法用途全部恰当；SHA-1 仅 ETag 缓存指纹非安全边界 + TOTP SHA1 为 RFC 标准）+ 第 867 轮全量回归 246/246 零失败
- 第七十二轮（868-869）：DOMPurify 消毒配置深核（白名单/协议/rel 加固/3.4.13 无 CVE —— 最后防线完整）+ 第 869 轮全量回归 246/246 零失败
- 第七十三轮（870-871）：长文渲染边界（内容 10 万字符上限有界 —— DOM 规模可控）+ 第 871 轮全量回归 246/246 零失败
- 第七十四轮（872-873）：路由参数校验完备性扫描（93 路由 —— 97.8% 自动化确认 + 2 条人工补核：文件名白名单/slug 参数化 —— 100% 安全）+ 第 873 轮全量回归 246/246 零失败
- 第七十五轮（874-875）：前端组件 props 全类型声明 + 第 875 轮全量回归 246/246 零失败
- 第七十六轮（876-877）：综合端到端验收 5/5（用户列表/仪表盘数据/安全中心/WAF 拦截/攻击后稳定 —— 三链路一次跑完）+ 第 877 轮全量回归 246/246 零失败
- WAF 模糊测试 1275 变体：拦截率 92.2%，剩余放行项均为不可利用误报（字面量 `\t`、绝对路径搜索词）
- 性能基线复查无回退：列表 14.5ms / 详情 9.8ms / 评论 6.4ms / 搜索 9.3ms
- 前后端 API 路径一致性核对通过；生产构建 23s 成功；npm audit 0 漏洞
- 数据库层：EXPLAIN 热路径全部走索引（列表/详情/评论/邻居/标签/UV/分类/会话），连接池 10s 获取超时防悬挂
- 谜题池 DoS 压测：300 并发谜题请求 → 限流放行 6 个、294 个 429 → 触发信誉封禁 → 全站 403（分层防御端到端验证）
- 构建产物核查：无 sourcemap、无密钥、无 env 变量（sk- 命中为骨架屏类名误报）；仪表盘自绘图表无 XSS 面
- 备份脚本：新增 `--no-tablespaces`（MySQL 8 最小权限账号零告警）；备份→恢复闭环验证通过（6 文章/3 评论/3 会话/1 用户完整还原）
- **连接级时区修复**：`SET SESSION time_zone` 改为连接创建时实时计算 —— 模块加载时计算的偏移在 DST 切换后过期（存量连接用旧偏移），连接级重设保证长期运行的服务器时间写入始终正确
- **密钥记忆化**：`resolveSecret()` 同一进程内多次调用返回同一值 —— 此前 jwt.secret 与 adminPath 派生各调一次，未配置 JWT_SECRET 时会生成两个不同随机密钥（进程内一致但属潜在隐患）；配置 JWT_SECRET 后跨重启稳定（验证 ✓）
- **正文解密续期竞态修复**：详情请求在途期间若触发 8 分钟票据自动续期，响应拦截器用「当前票据」解密会密钥不匹配 → 正文渲染为空。修复：请求拦截器记录本次请求所用票据（config._ticket），解密时优先用请求票据（验证：A 加密 → A 解 ✓ / B 解 ✗）
- **相对时间时区偏移修复**：timeAgo 把无时区后缀的本地时间字符串按访客浏览器时区解析 —— 服务器 +8 存储的时间在 UTC 访客端显示偏差 8 小时（1 小时前显示为 9 小时前）。修复：/api/settings 新增 server_tz_offset_min，前端按服务器偏移解析（UTC/+8 访客均验证正确；偏移未加载时短暂偏差，设置加载后自动校正）

## 第五轮验证记录（第 13-29 轮）
- 竞态实测：同表单令牌 3 并发 → 仅 1 成功（单次使用并发安全）；同标题 3 并发创建 → 仅 1 成功 + 2 个干净 400（slug 唯一约束）
- 注入实测：keyword 参数（WAF 豁免）6 种 SQL 载荷全部按字面文本处理（无延迟/UNION/堆叠）
- 票据防篡改：签名/jti/时间戳/重复头/畸形值全部 403
- 表单令牌绑定：跨路径（/comments→/messages）与错指纹提交均被拒
- CRLF 头注入：X-Request-Id 正则校验+随机化，Set-Cookie 注入未反射
- WAF 边界防御：越过 4000 字符检测切片的载荷被存储层 cleanText（2000 字符+标签剥离）拦截
- escapeLike 字节级验证：%/_\ 全部按 MySQL LIKE 语义转义（7 用例）
- 标签合并端到端：源标签删除+关联转移+无孤儿（事务验证）
- 时区显示一致性：全部 timeAgo 调用点核查（site/admin 共用实现；SecurityManage 基于 epoch 天然正确）
- 设置键注入：未知键忽略 + javascript: URL 被 WAF 拦截 + 协议白名单双层防御（实测）
- WAF 性能基准：常规请求 0.36ms/次、4KB 值 2.3ms/次（有界，无指数放大）
- emoji 往返完整性：😀🚀中文测试🎉 24 字节完整存储（utf8mb4 实测）
- 上传目录卫生：无孤儿文件；孤儿清理 24h 竞态保护实现核对
- SMTP 邮件：命令注入防护（CR/LF 清洗）+ 收件人格式校验 + 响应码门控复核
- 部署文档补齐环境要求（Node ≥20 / MySQL 8.x / Nginx 1.20+）；systemd 单元最小权限+自动重启核对
- **nginx 生产配置修复**：`client_max_body_size 6m`（与后端 5MB 上传限制对齐，默认 1MB 会导致 >1MB 图片上传被 413）+ `proxy_read/send_timeout 35s`（与后端慢速防护 30s 对齐）
- 留言回复邮件通知功能补齐（与评论回复同标准）+ 回复不存在留言返回 404
- 前端 429 限流提示从 error 改为 warning（非错误语义）
- 综合回归：UV 并发去重（insertId 判断）、外链审核收紧、时区偏移、密钥记忆化等全部包含在内 204/204 零失败
- **请求头防护实测**：Node 25 的 `server.maxHeadersCount` 属性方式 = 静默截断 req.headers 至 60（而非拒绝）—— 洪水被截断有界 + 16KB 大小限制 431 兜底；WAF 新增头数量检查作为未来 Node 行为变化的纵深防御；跨 IP 使用票据验证 403（票据 IP 绑定实弹确认）
- **第 92-150 轮验证**（累计）：CORS 预检/拒绝、时间戳窗口双向拒绝、签名路径/bodyHash 绑定、表单令牌最小间隔（不消耗令牌的用户友好设计）与单次使用、seed 限流共享、混合负载 32/32、RSS 双模式、审计清空留痕、gzip 47% 压缩、304 条件发送、票据 IP/UA/指纹三重绑定实弹、登录页已登录重定向（功能补齐）等全部通过
- **第 151-163 轮验证**：编码变体 XSS 全链路（WAF 拦截 6/6 + content 消毒链路 + DOMPurify 渲染中和）、蜜罐双命中即封禁、封禁时长指数升级（15min→24h 封顶）、封禁跨重启持久化（ip_bans 端到端）、组合爬虫行为模拟（UA+指纹轮换→集成封禁）、JS bundle 后台路径零泄露、前端封禁剩余时间提示（功能补齐）等全部通过
- **第 164-185 轮验证**：导入往返（去重跳过+新增落库）、仪表盘范围/钳制/零填充、待审实时、安全面板统计/持久化合并、审计搜索/导出/公式注入 E2E、设置备份往返、设置导入白名单与协议校验双层（WAF 键名检测 + saveSettings 白名单 + URL 协议校验；豁免语义确认：管理端设置字段豁免 WAF 值检测由协议校验兜底，用户面字段全量检测）等全部通过
- **第 186-230 轮验证**：攻击意图审核词库（Bug #17，正文+昵称双检 26 词零误伤）、AI-REJECT 全链路（判定→400→信誉→审计留痕）、垃圾累计难度联动、重复内容检测（429）、邮件隐私（Gravatar md5+字段剥离）、SMTP 注入清洗/收件人校验、TOTP 标准实现、登录锁定/会话/TOTP 防重放表、全 API 路由清单（43 公开+50 后台全覆盖）、草稿系统（TTL 24h）、深链注入防护（Number 先行转换）、前后端构建一致性等全部通过
- **第 231-250 轮验证**：登录响应字段卫生（无密码/密钥）、会话全生命周期（列表/撤销/401 生效/退出全部/改密撤销）、密码策略全项（长度/纯数字/纯字母/常见词/含用户名）、时序攻击防护（FAKE_HASH 同成本均衡）、2FA 流程（setup/otpauth 格式）、搜索转义一致性、快捷键/焦点陷阱/表单交互等全部通过
- **第 251-260 轮验证**：UA 摘要/会话管理 UI/头像上传流/上传-列表-删除 E2E、EXIF 剥离（APP1 移除+结构保留+保守回退）、SVG 服务防护链（attachment 下载+nosniff+CSP default-src 'none'）等全部通过
- **第 261-270 轮验证**：参数洪水（250 键 403）、嵌套/数组 query 与 body 攻击递归检测、JSON 炸弹（深度 30/数组 1500 拦截）、无票据读写拒绝、robots/security.txt 内容、security.txt Expires 动态化（RFC 9116 合规，Bug #18 级优化）等全部通过
- **第 271-310 轮验证**：数据库健康/外键/级联、文章复制（草稿+标签+slug）、分类删除 SET NULL/更新（含 Bug #18 hex 颜色修复）、标签删除、标签/分类/排序筛选、置顶切换、发布时间保护、文章 CRUD、评论批量操作、友链状态/SSRF 防护（含 hex/十进制 IP 规范化拒绝）、设置缓存失效/TTL、编辑器交互等全部通过
- **第 311-320 轮验证**：Bug #20（导入相对封面丢失）修复 + 完整往返（封面/分类/标签全保留）、设置全量导出导入（15 键）、默认值回退、RSS/Sitemap/分享页默认设置表现等全部通过
- **第 321-330 轮验证**：标签特殊字符/XSS 中和、文章后台搜索（标题+摘要+正文）、评论搜索/AI 标记筛选/AI 复核误拒恢复等全部通过
- **第 331-350 轮验证**：留言/友链管理全链路（复核/一键通过/批量删除/回复展示/分页）、后台评论分页/父级昵称、前台 15 端点冒烟、后台真实流程、分类/标签/归档/随机/邻居（含对称性）等全部通过
- **第 351-360 轮验证**：相关文章三级回退、相关阅读渲染、移动端交互（悬浮栏/评论区跳转/进度条/字号钳制/TOC 记忆）、Markdown 样式等全部通过
- **第 361-370 轮验证**：站点配置全链路（站名/URL 联动 RSS/分享/Sitemap、公告/页脚/ICP/社交/关于页/审核开关双通道）、仪表盘交互等全部通过
- **第 371-380 轮验证**：假爬虫反向 DNS 拒绝、搜索历史/收藏清理、首页各区块（热门切换/最新评论/分类入口）、关于页统计等全部通过
- **第 381-390 轮验证**：长度限制（评论 2000/正文 100KB/标题 200/标签 50/摘要 300-500）、上传类型/魔数/大小限制、引用图片删除保护（引用 400 + 解除后可删）等全部通过
- **第 391-400 轮验证**：孤儿清理 24h 窗口、上传列表引用标记、复制 Markdown 链接、仪表盘卡片跳转、PWA 图标/manifest、404 页（noindex/倒计时/随机文章）等全部通过
- **第 401-410 轮验证**：完整站点验收（前台读取/加密详情/评论/登录/后台/仪表盘/安全中心 7/7）、评论/点赞/浏览/PV/UV/摘要/趋势计数联动等全部通过
- **第 411-420 轮验证**：3 用户并发完整旅程（9 请求全 200 无数据竞争）、性能基线复查无回退（5-16ms）、RSS 媒体标签（enclosure/media:thumbnail 绝对化）、页脚外链安全（noopener）等全部通过
- **第 421-430 轮验证**：混合压力 20/20 零失败、移动端菜单/主题三态/强调色/持久化/系统跟随/过渡/防闪烁、打印样式等全部通过
- **第 431-440 轮验证**：综合安全验证 9/9（PoW/闸门/登录/弱密码/加密/隐私/WAF/蜜罐）、安全面板封禁列表/CSV 导出（双端公式防护）、2FA 状态/筛选器/改密提示等全部通过
- **第 441-450 轮验证**：API 面遍历 26/26、后台写操作面 8/8、管理写审计留痕/失败不入账、目录爆破检测（11×404 后 403）、分享/规范 URL 一致性、JSON-LD 注入等全部通过
- **第 451-460 轮验证**：设置特殊字符（emoji/换行/5000 截断）、social_email→security.txt 联动、关于页/默认值/后台入口/Hero/统计滚动/卡片流等全部通过
- **第 461-470 轮验证**：模块引用完整性（前后端）、自适应难度档位（0-3→16/4-7→17/8-11→18）、封禁态（/anti 豁免+带票仍拒）、Bug #21（IPv6 解封误伤）修复 + 同类 IP 变体全放行等全部通过
- **第 471-480 轮验证**：过期会话清理、会话上限 20、退出全部确认（功能补齐）、2FA 关闭需码、会话过期显示、审计方法着色/搜索、AI 标记显示等全部通过
- **第 481-490 轮验证**：Gravatar 隐私 E2E（邮箱剥离+md5 确定性）、后台邮箱权限、pageSize/视图记忆、序号/空态/重试/加载态等全部通过
- **第 491-497 轮验证**：主题色 hex 校验/默认色/响应式应用/循环切换/损坏值安全降级、最终状态双确认（204 断言全绿 + 构建成功）等全部通过
- **第 498-500 轮验证**：反爬全栈联动（正常→WAF 拒绝→计分→提难→自动封禁）、项目结构完整性、500 轮里程碑（204 断言全绿）等全部通过
- **第 501-510 轮验证**：注册面封闭（单管理员制）、角色处理/JWT 篡改/会话层拒绝、登录锁定提示、登录表单/密码可见性/auth store 等全部通过
- **第 511-520 轮验证**：前端重试循环守卫、API 超时/dev 代理/构建 base/前缀一致性、CORS 实际响应头（ACAO+Vary）、API 缓存头（no-store+CSP+CORP）、上传文件缓存（7 天 immutable+CORP 跨域）等全部通过
- **第 521-530 轮验证**：安全头全端点遍历（13/13 全齐）、CSP 双端 frame-ancestors、robots/security.txt/sitemap 合规性等全部通过
- **第 531-540 轮验证**：时区全链路一致、日期/字数/阅读时长（含 Bug #22 修复）/浏览格式化/JSON-LD 双日期/打印等全部通过
- **第 541-550 轮验证**：评论/留言/首页/仪表盘/后台时间显示全站点一致性（timeAgo 7 处 + formatDateTime 4 处 + formatDate 10 处）、timeAgo 边界行为等全部通过
- **第 551-560 轮验证**：前后台完整流复查（9/9+7/7）、退出令牌失效、登录/退出/审计事件链路（AUTH 前缀/字段完整/搜索/导出）等全部通过
- **第 561-565 轮验证**：完整链路最后复查（公开+认证+管理+导出 8/8）、服务/连接稳定性、暗色模式（data-theme 11 处）等全部通过
- **第 566-569 轮验证**：360° 用户旅程（7/7）、设置头像预览、最终数据完整性（14 表 3195 行）、最终全量回归（204 断言全绿）等全部通过
- **第 570-578 轮验证**：最终验收（8/8）、meta/OG/Twitter/manifest/语言/标题/SEO 描述一致性、最终双确认（204 全绿 + 构建成功）等全部通过
- **第 579-580 轮验证**：累积修复确认（Bug #18/20/21 全部生效）、580 轮里程碑（204 断言全绿）等全部通过
- **第 581-582 轮验证**：六层纵深防御最终遍历（WAF/闸门/认证/加密/蜜罐/封禁全生效）、最终确认（204 全绿 + 构建成功）等全部通过
- **第 583-590 轮验证**：最终全面验收（6/6）、种子数据/首页数据完整、未分类统计/测试残留恢复、590 轮里程碑（204 断言全绿）等全部通过
- **第 591-598 轮验证**：最终状态快照（交付基线）、构建产物核验（110 文件 2.03MB 无 map）、双端依赖审计（Bug #23 dompurify XSS 修复后 0 漏洞）、最终回归（204 断言全绿）等全部通过
- **第 599-600 轮验证**：600 轮前全面验收（7/7）、600 轮里程碑（204 断言全绿）等全部通过
- **第 601-606 轮验证**：并发写无丢失更新、关键修复抽查（Bug #13/14/15/18/20/21 全生效 7/7）、核心链路（7/7）、最终定义回归（204 断言全绿 + 构建成功）等全部通过
- **第 607-610 轮验证**：回归状态确认（6/6）、交付物完整性（13/13）、数据健康（14 表/1 用户/6 文章）、610 轮状态锁定等全部通过
- **第 611-615 轮验证**：机制清单全确认 —— 闸门 9/9（PoW/TTL/续期/签名/指纹/UA/池/代理池/爬虫）、WAF 14/14（注入/XSS/遍历/命令/SSRF/扫描器/蜜罐/走私/炸弹/洪水/轮换/头/键）、IP 信誉 8/8、认证 5/5、内容安全 8/8、汇总 4/4 等全部通过
- 性能基线：列表 9.8ms / 详情 6.8ms / 评论 4.0ms / 搜索 6.8ms
- 并发 50 请求 408ms 全成功；3 并发用户无数据竞争
- DB 停机：快速 500 + 健康检查报 down + 服务不崩溃 + 自动恢复
- 时区一致性、索引覆盖、模块加载 42/42 无副作用、migrate 幂等
